import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  width: 60vw;
  max-width: 60vw;
  justify-content: center;
  align-items: center;

  z-index: 1;
  overflow-x: hidden;
  display: relative;
  margin: 0;
`;
